---
title: "Dados de Área"
subtitle: "Estatística Espacial"
author: "Prof. Wagner Tassinari"
format:
  revealjs:
    theme: default
    slide-number: true
    chalkboard: true
    transition: convex
    background-transition: fade
    highlight-style: tango
    code-background: true
    code-line-numbers: false
    code-copy: true
    title-slide-attributes:
      data-background-color: "#2c3e50"
---

## Objetivos do Capítulo

Ao final deste capítulo, você será capaz de:

::: {.large}
1. Construir e interpretar **mapas temáticos** (coropléticos).
2. Calcular e interpretar medidas de **autocorrelação espacial global** (I de Moran).
3. Aplicar a análise de **autocorrelação espacial local** (LISA).
4. Utilizar o estimador de **Bayes Empírico** para suavização de taxas.
5. Ajustar **modelos de regressão espacial** (SAR, CAR e GWR).
:::

---

## O que são Dados de Área?

#### Definição

Fenômenos **agregados por unidades geográficas** discretas, como municípios, estados e ou setores censitários.

#### Características

- Representados por **polígonos**.
- Objetivo: Identificar **padrões espaciais**.


---

## Mapa Temático

<div style="font-size: 70%;">
O mapa temático tem como principal objetivo visualizar e analisar a distribuição espacial de um fenômeno específico.
</div>

```{r, echo=T, out.width='90%', message=FALSE, warning=FALSE}
#| eval: false  # não executa o código
#| echo: true   # mostra o código
#| 
library(tmap)
data(World)
tmap_style("classic")

# Desenhando um rápido mapa temático mundial para esperança de vida.
qtm(World, fill = "life_exp")
```

## Mapa Temático

<div style="font-size: 70%;">
O mapa temático tem como principal objetivo visualizar e analisar a distribuição espacial de um fenômeno específico.
</div>

```{r}
#| fig-width: 12
#| fig-height: 8
#| out-width: 100%
#| fig-align: center
#| 
library(tmap)
data(World)
tmap_style("classic")

# Desenhando um rápido mapa temático mundial para esperança de vida.
qtm(World, fill = "life_exp")
```

## Matriz de Vizinhança

#### O que é?

A matriz de vizinhança (ou proximidade) é um instrumento fundamental que representa formalmente quais áreas são consideradas **vizinhas** entre si.

- Transforma o mapa em uma estrutura matemática.
- Por exemplo, se dois municípios são vizinhos, valor **1**; se não, valor **0**.
- A diagonal principal é zero (um município não é vizinho de si mesmo).


## Matriz de Vizinhança: Exemplo 1

![](figuras/vizinhanca.png){fig-align="center" width="40%"}

<div style="text-align: center; font-size: 14px;">
Exemplo de contiguidade do tipo Rainha (compartilham qualquer ponto de fronteira)
</div>

## Matriz de Vizinhança: Exemplo 2

<div style="font-size: 80%;">
**Municípios analisados:**  
  Rio de Janeiro, Niterói, São Gonçalo, Duque de Caxias, Nova Iguaçu  

**Critério:** Contiguidade do tipo *queen* (*Rainha*)  
  (considera vizinhos os municípios que compartilham qualquer ponto de fronteira)


|            | Rio | Niterói | S. Gonçalo | D. Caxias | N. Iguaçu |
  |:-----------|:---:|:-------:|:----------:|:---------:|:---------:|
  | **Rio**        |  0  |   1    |     0      |     1     |     1     |
  | **Niterói**    |  1  |    0    |     1      |     0     |     0     |
  | **S. Gonçalo** |  0  |    1    |     0      |     0     |     0     |
  | **D. Caxias**  |  1  |    0    |     0      |     0     |     1     |
  | **N. Iguaçu**  |  1  |    0    |     0      |     1     |     0     |
  
</div>

## Matriz de Vizinhança

<div style="font-size: 80%;">

Essa matriz é essencial porque muitos fenômenos da gestão pública apresentam dependência espacial. Ou seja, o que acontece em um município pode influenciar municípios vizinhos. Exemplos incluem:
  
- Propagação de doenças (como dengue);
- Dinâmica do desemprego;
- Criminalidade;
- Poluição ambiental;
- Expansão urbana.

Sem a matriz de vizinhança, os métodos estatísticos utilizados tratariam cada município como independente. Com ela, conseguimos incorporar a ideia de que “o território importa”.
</div>

## Tipos de Matrizes de Vizinhança no R

<div style="font-size: 80%;">
O pacote `spdep` oferece vários tipos de matrizes:

1. **Vizinhança por Contiguidade (Adjacência)**
   - **Rainha (Queen)**: Compartilham qualquer ponto de fronteira.
   - **Torre (Rook)**: Compartilham apenas fronteira lateral.
   
2. **Vizinhança por Distância**
   - **Raio Fixo**: Dentro de uma distância mínima e máxima.
   - **k-Vizinhos Mais Próximos**: Os *k* mais próximos, independente da distância.

3. **Vizinhança por Gráficos Geométricos**
   - **Delaunay** e **Voronoi**.
</div>


## Autocorrelação Espacial

#### Moran Global

<div style="font-size: 80%;">

Avalia se existe autocorrelação espacial em **todo o território** analisado.

$$I = \frac{ \sum_{i=1}^{n} \sum_{j=1}^{n} w_{ij} (y_i - \bar{y})(y_j - \bar{y}) }{ \sum_{i=1}^{n} (y_i - \bar{y})^2 }$$

- **I > 0**: Valores similares estão próximos (agrupamento positivo).
- **I < 0**: Valores diferentes estão próximos (dispersão).
- **I ≈ 0**: Padrão aleatório.

Indica se o fenômeno possui organização territorial, sugerindo a necessidade de políticas regionais integradas.

</div>


## Autocorrelação Espacial Local

#### Moran Local (LISA)

<div style="font-size: 80%;">

O Moran Local, também conhecido como LISA (*Local Indicators of Spatial Association*), avalia a autocorrelação espacial em **cada unidade territorial** individualmente.

$$I^{(i)} = \frac{n}{\sum_{j=1}^{n} (z_j - \bar{z})^2} \sum_{j=1}^{n} w_{ij} (z_i - \bar{z})(z_j - \bar{z})$$

- Identifica onde exatamente estão os agrupamentos espaciais e possíveis áreas atípicas.
- Classifica cada município como parte de um cluster de valores altos, baixos ou como um ponto fora do padrão.

</div>

## Autocorrelação Espacial Local

#### LISA Map

<div style="font-size: 80%;">

O LISA Map é um mapa temático que representa graficamente os resultados do Índice de Moran Local, ele identifica:
  
- Clustering local (agrupamento de valores semelhantes)

- Outliers espaciais (valores discrepantes em relação aos vizinhos)

- Regiões não significativas (sem padrão espacial detectável)

</div>

## Autocorrelação Espacial Local

#### LISA Map: Tipos de Associação

<div style="font-size: 70%;">

| Tipo | Descrição | Interpretação |
|------|-----------|---------------|
| 🔴 **Alto-Alto** | Valor alto cercado por valores altos | **Hotspot** (cluster de altas magnitudes) |
| 🔵 **Baixo-Baixo** | Valor baixo cercado por valores baixos | **Coldspot** (cluster de baixas magnitudes) |
| 🟠 **Alto-Baixo** | Valor alto cercado por valores baixos | **Outlier espacial positivo** |
| 🟡 **Baixo-Alto** | Valor baixo cercado por valores altos | **Outlier espacial negativo** |
| ⚪ **Não sig.** | Sem associação espacial relevante | Sem padrão espacial detectável |

</div>

## Exemplo de LISA Map

<div style="font-size: 70%;">
Esse mapa abaixo pertence ao artigo [COVRE, Eduardo Rocha et al. Correlação espacial da covid-19 com leitos de unidades de terapia intensiva no Paraná. Revista de Saúde Pública, v. 56, p. 14, 2022.](https://www.scielo.br/j/rsp/a/NDB7dYnVxgbtWPFskqxMgKR/?lang=pt)

O artigo teve por objetivo analisar a autocorrelação espacial dos casos confirmados de covid-19 em municípios sede de regionais de saúde e vizinhos, segundo a análise do Índice Local de Moran (LISA) Univariado. Paraná, Brasil, março de 2020 a janeiro de 2021.
</div>

![](figuras/moran_local3_covid.png){fig-align="center" width="60%"}

## Aplicação I: AEDE

<div style="font-size: 80%;">

#### Análise Exploratória de Dados Espaciais (AEDE)

```{r, echo=T, out.width='90%'}
library(readxl)
library(dplyr)

dengue <- read_excel("dados/dengue/dengue2025.xlsx") |>
    mutate(code_muni  = as.character(code_muni),
           casos  = as.numeric(casos),
           pop2022 = as.numeric(pop2022), 
           incidencia = round(casos / pop2022 * 100000, 1))

head(dengue)
```
</div>

## Aplicação I: AEDE

<div style="font-size: 80%;">

##### Baixando via `geobr`

```{r, echo=T, out.width='90%', message=FALSE, warning=FALSE, comments=NA}
library(geobr)
# Baixar a malha municipal do estado do Rio de Janeiro
# code_muni = 33 (código do estado do RJ)
mapa_rj <- read_municipality(code_muni = 33, year = 2024) |>
        mutate(code_muni = as.character(code_muni))
head(mapa_rj)
```
</div>

## Aplicação I: AEDE

<div style="font-size: 80%;">

##### Primeiro, vamos fazer um gráfico apenas com as geometrias.

```{r, echo=T, out.width='90%'}
library(ggplot2)
# Visualizar rapidamente o mapa
ggplot(mapa_rj) + 
  geom_sf()
```

</div>

## Aplicação I: AEDE

<div style="font-size: 70%;">

Agora precisamos conectar a tabela de casos de dengue com a malha municipal do RJ. Para isso, utilizamos a função `left_join()` do pacote `dplyr`, utilizando a variável `code_muni` como chave de ligacao.


```{r, echo=T, out.width='90%'}
mapa_rj <- mapa_rj |>
  mutate(code_muni = as.character(code_muni)) |>
  left_join(dengue, by = "code_muni") |> 
  select(code_muni, municipio, pop2022, casos, incidencia)  

head(mapa_rj)
```
</div>

## Aplicação I: AEDE

<div style="font-size: 80%;">

Agora sim vamos plotar o mapa temático de forma contínua com as incidências de dengue por município do RJ.

```{r}
#| eval: false  # não executa o código
#| echo: true   # mostra o código

library(ggplot2)

ggplot() +
  geom_sf(data = mapa_rj, aes(fill = incidencia), color = "white", size = 0.2) +
  scale_fill_viridis_c(option = "plasma", 
                       name = "Incidência\n(por 100.000 hab)",
                       direction = -1) +  # direction = -1 inverte a escala
  theme_minimal() +
  labs(title = "Incidência de Casos nos Municípios do RJ - 2025",
       subtitle = "Escala Contínua",
       caption = "Fonte: Dados Simulados") +
  theme(legend.position = "right",
        plot.title = element_text(hjust = 0.5, face = "bold"),
        plot.subtitle = element_text(hjust = 0.5),
        axis.text = element_blank(),
        axis.title = element_blank())     
```
</div>

## Aplicação I: AEDE

<div style="font-size: 80%;">

```{r}
#| fig-width: 12
#| fig-height: 8
#| out-width: 100%
#| fig-align: center
library(ggplot2)

ggplot() +
  geom_sf(data = mapa_rj, aes(fill = incidencia), color = "white", size = 0.2) +
  scale_fill_viridis_c(option = "plasma", 
                       name = "Incidência\n(por 100.000 hab)",
                       direction = -1) +  # direction = -1 inverte a escala
  theme_minimal() +
  labs(title = "Incidência de Casos nos Municípios do RJ - 2025",
       subtitle = "Escala Contínua",
       caption = "Fonte: Dados Simulados") +
  theme(legend.position = "right",
        plot.title = element_text(hjust = 0.5, face = "bold"),
        plot.subtitle = element_text(hjust = 0.5),
        axis.text = element_blank(),
        axis.title = element_blank())     
```

</div>

## Aplicação I: AEDE

<div style="font-size: 80%;">

Sumário estatístico da incidência de dengue por município

```{r, echo=T, out.width='90%'}
summary(mapa_rj$incidencia)
```
</div>

## Aplicação I: AEDE

<div style="font-size: 80%;">

Agora vamos plotar o mapa temático de forma categórica pelos quantis com as incidências de dengue por município do RJ.

```{r}
#| eval: false  # não executa o código
#| echo: true   # mostra o código

# Criar categorias usando 5 classes com método quantil
library(viridis)

map_data <- mapa_rj %>%
  mutate(
    incidencia_cat = cut(incidencia,
                         breaks = c(0, 51.7, 723.5, 1791.9, 2375.7, 3779.5, 7458.7, Inf),
                         labels = c("Mín - 51.7", 
                                   "51.7 - 723.5", 
                                   "723.5 - 1791.9", 
                                   "1791.9 - 2375.7", 
                                   "2375.7 - 3779.5", 
                                   "3779.5 - 7458.7",
                                   "> 7458.7"),
                         include.lowest = TRUE,
                         right = FALSE)
  )

ggplot() +
  geom_sf(data = map_data, aes(fill = incidencia_cat), color = "white", size = 0.2) +
  scale_fill_manual(values = viridis(7, option = "plasma", direction = -1),
                    name = "Incidência\n(por 100.000 hab)",
                    na.translate = FALSE) +
  theme_minimal() +
  labs(title = "Incidência de Casos nos Municípios do RJ",
       subtitle = "Classificação por Quantis",
       caption = "Fonte: Dados Simulados") +
  theme(legend.position = "right",
        plot.title = element_text(hjust = 0.5, face = "bold"),
        plot.subtitle = element_text(hjust = 0.5),
        axis.text = element_blank(),
        axis.title = element_blank())  
```

</div>

## Aplicação I: AEDE

<div style="font-size: 80%;">

```{r}
#| fig-width: 12
#| fig-height: 8
#| out-width: 100%
#| fig-align: center
library(ggplot2)

library(viridis)

map_data <- mapa_rj %>%
  mutate(
    incidencia_cat = cut(incidencia,
                         breaks = c(0, 51.7, 723.5, 1791.9, 2375.7, 3779.5, 7458.7, Inf),
                         labels = c("Mín - 51.7", 
                                   "51.7 - 723.5", 
                                   "723.5 - 1791.9", 
                                   "1791.9 - 2375.7", 
                                   "2375.7 - 3779.5", 
                                   "3779.5 - 7458.7",
                                   "> 7458.7"),
                         include.lowest = TRUE,
                         right = FALSE)
  )

ggplot() +
  geom_sf(data = map_data, aes(fill = incidencia_cat), color = "white", size = 0.2) +
  scale_fill_manual(values = viridis(7, option = "plasma", direction = -1),
                    name = "Incidência\n(por 100.000 hab)",
                    na.translate = FALSE) +
  theme_minimal() +
  labs(title = "Incidência de Casos nos Municípios do RJ",
       subtitle = "Classificação por Quantis",
       caption = "Fonte: Dados Simulados") +
  theme(legend.position = "right",
        plot.title = element_text(hjust = 0.5, face = "bold"),
        plot.subtitle = element_text(hjust = 0.5),
        axis.text = element_blank(),
        axis.title = element_blank())  
```

</div>

## Aplicação I: Matrix de Vizinhança

<div style="font-size: 80%;">

```{r, echo=T, out.width='90%'}
library(spdep)

# Converter para objeto sp (formato exigido por alguns pacotes)
rj_sp <- as(mapa_rj, "Spatial")

# Criar lista de vizinhos baseada em contiguidade (critério "rainha" - queen)
vizinhos <- poly2nb(mapa_rj, queen = TRUE)

# Verificar o resultado
summary(vizinhos)
```

</div>


## Aplicação I: Matrix de Vizinhança

<div style="font-size: 80%;">

```{r, echo=T, out.width='90%'}
# Converter lista de vizinhos para matriz binária
matriz_vizinhos <- nb2mat(vizinhos, style = "B", zero.policy = TRUE)

# Visualizar as primeiras linhas e colunas da matriz
print(matriz_vizinhos[1:10, 1:10])

# Verificar dimensões (deve ser 92x92, pois são 92 municípios)
dim(matriz_vizinhos)

```

</div>

## Aplicação I: Matrix de Vizinhança

<div style="font-size: 70%;">

Plotando a malha de conectividade da vizinhança da cidade do Rio de Janeiro/RJ

```{r, echo=T,  out.width="70%"}
# convertendo em formato sp para extrair as coordenadas dos centroides
library(sp)
mapa_rj.sp <- as(mapa_rj, 'Spatial') 
# coordenadas dos centroides dos poligonos do RJ
coord <- coordinates(mapa_rj.sp) 
class(mapa_rj.sp)
```

Convertendo em uma lista de vizinhos espaciais (vizinhos) em linhas e depois transforma em um objeto `sf` (geometria de linhas) e depois atribui ao objeto `viz.sf` o mesmo sistema de coordenadas (CRS) do mapa do Rio de Janeiro para garantir que ambos estejam no mesmo referencial espacial.

```{r, echo=T}
# Convertendo em uma lista de vizinhos espaciais 
viz.sf <- as(nb2lines(vizinhos, coords = coord), 'sf')
# Atribuindo o mesmo sistema de coordenadas (CRS) 
viz.sf <- st_set_crs(viz.sf, st_crs(mapa_rj))
```

</div>

## Aplicação I: Matrix de Vizinhança

<div style="font-size: 80%;">

Plotando o mapa do estado do Rio de Janeiro com o grafo de conectividade por contiguidade.

```{r}
#| eval: false  # não executa o código
#| echo: true   # mostra o código

mapa.viz <- ggplot(mapa_rj) + 
  geom_sf(fill = 'lightpink', color = 'white') +
  geom_sf(data = viz.sf) +
  theme_minimal() +
  ylab("Latitude") +
  xlab("Longitude")
mapa.viz
```

</div>

## Aplicação I: Matrix de Vizinhança

<div style="font-size: 80%;">

```{r}
#| fig-width: 12
#| fig-height: 8
#| out-width: 100%
#| fig-align: center

# Plotando o grafo de conectividade por contiguidade
mapa.viz <- ggplot(mapa_rj) + 
  geom_sf(fill = 'lightpink', color = 'white') +
  geom_sf(data = viz.sf) +
  theme_minimal() +
  ylab("Latitude") +
  xlab("Longitude")
mapa.viz
```

</div>


## Aplicação I: Autocorrelação espacial

<div style="font-size: 80%;">

```{r, echo=T, out.width='90%'}
pesos.viz <- nb2listw(vizinhos)
moran.test(mapa_rj$casos, pesos.viz)
```

Há autocorrelação espacial positiva significativa ($p-valor < 0,05$), ou seja, vizinhos tendem a ter valores parecidos. Mas o índice é baixo ($I = 0,032$), mostrando uma intensidade fraca desse agrupamento.

</div>

## Aplicação I: Autocorrelação espacial

<div style="font-size: 80%;">

Mapeando os polígonos que tiveram os p-valores mais significativos no Moran Local para os casos.

```{r}
#| eval: false  # não executa o código
#| echo: true   # mostra o código
#| 
mapa_rj$pval <- localmoran(mapa_rj$casos, pesos.viz)[,5]

library(tmap)

# tmap_mode("plot")  # modo estático
tmap_mode("view")  # modo interativo

tm_shape(mapa_rj) + 
  tm_polygons(
    col = "pval",
    title = "p-valores",
    breaks = c(0, 0.01, 0.05, 0.10, 1),
    fill.legend = tm_legend(title = "Casos"),
    lwd = 0.5,
    style = "fixed",
    palette = "-Red"
  )


```

</div>

## Aplicação I: Autocorrelação espacial

<div style="font-size: 80%;">

```{r}
#| fig-width: 14
#| fig-height: 6
#| out-width: 80%
#| fig-align: center
#| 
mapa_rj$pval <- localmoran(mapa_rj$casos, pesos.viz)[,5]

library(tmap)

# tmap_mode("plot")  # modo estático
tmap_mode("view")  # modo interativo

tm_shape(mapa_rj) + 
  tm_polygons(
    col = "pval",
    title = "p-valores",
    breaks = c(0, 0.01, 0.05, 0.10, 1),
    fill.legend = tm_legend(title = "Casos"),
    lwd = 0.5,
    style = "fixed",
    palette = "-Red"
  )

```

</div>

<!-- ## Aplicação I: Autocorrelação espacial -->

<!-- <div style="font-size: 80%;"> -->

<!-- Aqui vamos calcular o $I$ de Moran local (para cada município) usando um teste estatístico robusto chamado SAD (Simulação sob Hipótese de Aleatoriedade). O resultado mostra quais municípios têm autocorrelação espacial significativa. -->

<!-- ```{r, echo=T} -->
<!-- resI <- localmoran.sad(lm(mapa_rj$casos ~ 1), 1:length(vizinhos), vizinhos, style = "W") -->
<!-- summary(resI)[1:10,] -->
<!-- ``` -->

<!-- </div> -->


## Aplicação I: Autocorrelação espacial

<div style="font-size: 80%;">

##### LISA Map dos casos de dengue na cidade do Rio de Janeiro/RJ

```{r}
#| eval: false  # não executa o código
#| echo: true   # mostra o código

library(spdep)

#  Convertendo uma estrutura de vizinhança (objeto nb) em uma matriz de pesos espaciais padronizada
w <- nb2listw(vizinhos, style = "W")

# Matriz de pesos espaciais padronizada (row-standardized)
resI <- localmoran(mapa_rj$casos, nb2listw(vizinhos, style = "W"))

# Armazena estatísticas no objeto espacial
mapa_rj$Ii       <- resI[,1]   # valor de Moran Local
mapa_rj$pvalue   <- resI[,5]   # p-valor

# Valor padronizado da variável tx
mapa_rj$z_casos <- scale(mapa_rj$casos)[, 1]

# Média dos vizinhos (lagged value)
lag_casos <- lag.listw(w, mapa_rj$z_casos)

# Classificação dos tipos de associação local dos clusters
mapa_rj$cluster_type <- "Não significativo"

mapa_rj$cluster_type[mapa_rj$pvalue <= 0.05 & mapa_rj$z_casos > 0 & lag_casos > 0] <- "Alto-Alto"
mapa_rj$cluster_type[mapa_rj$pvalue <= 0.05 & mapa_rj$z_casos < 0 & lag_casos < 0] <- "Baixo-Baixo"
mapa_rj$cluster_type[mapa_rj$pvalue <= 0.05 & mapa_rj$z_casos > 0 & lag_casos < 0] <- "Alto-Baixo"
mapa_rj$cluster_type[mapa_rj$pvalue <= 0.05 & mapa_rj$z_casos < 0 & lag_casos > 0] <- "Baixo-Alto"
```

</div>

## Aplicação I: Autocorrelação espacial

<div style="font-size: 80%;">

##### LISA Map dos casos de dengue na cidade do Rio de Janeiro/RJ

```{r}
#| eval: false  # não executa o código
#| echo: true   # mostra o código

library(tmap)

# Modo estático (ideal para relatórios impressos ou PDF)
tmap_mode("plot")

# Mapas interativos são ótimos para exploração
# tmap_mode("view")

# Geração do mapa_rj LISA
tm_shape(mapa_rj) +
  tm_fill(
    col = "cluster_type",
    palette = c(
      "Alto-Alto"         = "#E60000",  # vermelho forte
      "Baixo-Baixo"       = "#0033CC",  # azul escuro
      "Baixo-Alto"        = "#9999FF",  # azul claro
      "Alto-Baixo"        = "#FF9999",  # rosa claro
      "Não significativo" = "#FFFFFF"   # branco
    ),
    title = "Cluster Local (LISA)",
    legend.is.portrait = TRUE
  ) +
  tm_borders(col = "gray40", lwd = 0.4) +
  tm_layout(
    main.title = "LISA map - Clusters Locais dos Casos",
    main.title.size = 1.2,
    legend.outside = TRUE,
    frame = FALSE,
    bg.color = "white"
  )

```

</div>


## Aplicação I: Autocorrelação espacial

<div style="font-size: 80%;">

```{r}
#| fig-width: 11
#| fig-height: 6
#| out-width: 100%
#| fig-align: center

library(spdep)

#  Convertendo uma estrutura de vizinhança (objeto nb) em uma matriz de pesos espaciais padronizada
w <- nb2listw(vizinhos, style = "W")

# Matriz de pesos espaciais padronizada (row-standardized)
resI <- localmoran(mapa_rj$casos, nb2listw(vizinhos, style = "W"))

# Armazena estatísticas no objeto espacial
mapa_rj$Ii       <- resI[,1]   # valor de Moran Local
mapa_rj$pvalue   <- resI[,5]   # p-valor

# Valor padronizado da variável tx
mapa_rj$z_casos <- scale(mapa_rj$casos)[, 1]

# Média dos vizinhos (lagged value)
lag_casos <- lag.listw(w, mapa_rj$z_casos)

# Classificação dos tipos de associação local dos clusters
mapa_rj$cluster_type <- "Não significativo"

mapa_rj$cluster_type[mapa_rj$pvalue <= 0.05 & mapa_rj$z_casos > 0 & lag_casos > 0] <- "Alto-Alto"
mapa_rj$cluster_type[mapa_rj$pvalue <= 0.05 & mapa_rj$z_casos < 0 & lag_casos < 0] <- "Baixo-Baixo"
mapa_rj$cluster_type[mapa_rj$pvalue <= 0.05 & mapa_rj$z_casos > 0 & lag_casos < 0] <- "Alto-Baixo"
mapa_rj$cluster_type[mapa_rj$pvalue <= 0.05 & mapa_rj$z_casos < 0 & lag_casos > 0] <- "Baixo-Alto"

library(tmap)

# Modo estático (ideal para relatórios impressos ou PDF)
# tmap_mode("plot")

# Mapas interativos são ótimos para exploração
tmap_mode("view")

# Geração do mapa_rj LISA
tm_shape(mapa_rj) +
  tm_fill(
    col = "cluster_type",
    palette = c(
      "Alto-Alto"         = "#E60000",  # vermelho forte
      "Baixo-Baixo"       = "#0033CC",  # azul escuro
      "Baixo-Alto"        = "#9999FF",  # azul claro
      "Alto-Baixo"        = "#FF9999",  # rosa claro
      "Não significativo" = "#FFFFFF"   # branco
    ),
    title = "Cluster Local (LISA)",
    legend.is.portrait = TRUE
  ) +
  tm_borders(col = "gray40", lwd = 0.4) +
  tm_layout(
    main.title = "LISA map - Clusters Locais dos Casos",
    main.title.size = 1.2,
    legend.outside = TRUE,
    frame = FALSE,
    bg.color = "white"
  )
```


</div>

## Modelos de Regressão Espacial

A hipótese de independência das observações em geral é **falsa** para dados espaciais. Existe dependência espacial!

Se existir dependência, devemos incluir no modelo os **Efeitos Espaciais**, caso contrário, as estimativas podem ficar enviesadas.

### Como verificar?
Analisar os resíduos da regressão tradicional (OLS) com o índice de Moran.

---

## Modelos Globais vs. Locais

### Modelos Globais (SAR, CAR)
Assumem que a estrutura espacial é **constante** em todo o espaço geográfico. Utilizam um único parâmetro para captar a autocorrelação.
- **SAR**: Dependência na variável dependente.
- **CAR**: Dependência na estrutura do erro.

### Modelos Locais (GWR)
A Regressão Geograficamente Ponderada (GWR) permite que os coeficientes da regressão **variem ao longo do espaço**, ajustando uma equação específica para cada localidade.

---

## Exemplo Prático: GWR no R

<div style="font-size: 70%;">
O modelo GWR ajusta regressões locais, ponderando as observações pela distância.
</div>

```{r, eval=FALSE, echo=TRUE}
library(spgwr)

# Definindo a largura de banda (bandwidth)
bwG <- gwr.sel(media_frp ~ media_dias_sem_chuva, 
               data = mapa_rj, 
               coords = cbind(mapa_rj$lon, mapa_rj$lat), 
               adapt = TRUE)

# Ajustando o modelo GWR
queimadas.gwr <- gwr(media_frp ~ media_dias_sem_chuva, 
                     data = mapa_rj, 
                     coords = cbind(mapa_rj$lon, mapa_rj$lat), 
                     adapt = bwG, 
                     hatmatrix = TRUE, 
                     se.fit = TRUE)
```

---

## Comparação de Modelos

| Modelo | Adesão à distribuição real | Interpretação |
|--------|----------------------------|---------------|
| **LM** | ❌ Fraca | Perda de variabilidade e deslocamento da média. |
| **CAR** | ✅ Razoável | Aproxima-se da observada, mas apresenta suavização. |
| **GWR** | ✅✅ Melhor | Preserva melhor a dispersão e a heterogeneidade espacial. |

<div style="font-size: 70%; margin-top: 20px;">
O modelo GWR é frequentemente superior por capturar a variabilidade local que os modelos globais ignoram.
</div>

---

## Resumo do Capítulo

1. **Dados de Área** são fundamentais para analisar fenômenos agregados.
2. A **Matriz de Vizinhança** é a base matemática para a estatística espacial de área.
3. O **Moran Global** indica se há padrão espacial geral, enquanto o **Moran Local (LISA)** aponta onde estão os clusters.
4. Modelos tradicionais de regressão falham ao ignorar a dependência espacial.
5. Modelos espaciais como **CAR** e **GWR** corrigem vieses e revelam dinâmicas locais.

---

## Referências Sugeridas

<div style="font-size: 60%;">
- Waller, L. A., & Gotway, C. A. (2004). *Applied Spatial Statistics for Public Health Data*. Wiley-Interscience.
- Bivand, R. S., Pebesma, E., & Gomez-Rubio, V. (2013). *Applied Spatial Data Analysis with R* (2nd ed.). Springer.
- Cressie, N. (1993). *Statistics for Spatial Data* (Revised edition). Wiley.
- Druck, S., et al. (eds). (2004). *Análise Espacial de Dados Geográficos*. EMBRAPA.
- Fotheringham, A. S., Brunsdon, C., & Charlton, M. (2002). *Geographically Weighted Regression*. Wiley.
- Lovelace, R., Nowosad, J., & Muenchow, J. (2021). *Geocomputation with R*.
</div>
